const age: number = 23

const firstName: string = 'John'

const isMarried: boolean = false

const hobbies: string[] = ['Sports', 'Cooking']

const address: { city: string, postalCode: number } = {
    city: 'London',
    postalCode: 123456
}